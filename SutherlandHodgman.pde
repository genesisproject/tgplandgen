static class SutherlandHodgman {
  static PVector[] clip(PVector[] polygon, PVector[] clip) {
    ArrayList<PVector> result = new ArrayList<PVector>();
    int l = clip.length;
    for (int i=0; i<l; i++) {
    }
    return result.toArray(new PVector[result.size()]);
  }
}